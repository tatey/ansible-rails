---
- hosts: all
  sudo: true

  tasks:
    - name: dotenv | env
      template: src=templates/env.j2
                dest={{ app_dir }}/shared/.env
                owner=deploy
                group=deploy
                mode=644
